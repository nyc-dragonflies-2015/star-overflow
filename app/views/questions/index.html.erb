<h1>Welcome to the Questions Page</h1>

<ol><%@questions.each do |question| %>
    <li><%= link_to question.title, question_path(question.id) %><% if question.user_id == session[:user_id] %>
  | <%=link_to "Update Question", edit_question_path(question.id)%><% end %>

    <% end %></ol>